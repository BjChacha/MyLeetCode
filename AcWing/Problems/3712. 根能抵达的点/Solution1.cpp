#include<bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int T, N, Y, U, V, W;
    cin >> T;
    while (T--) {
        cin >> N >> Y;
        for (int n = 0; n < N - 1; n++) {
            cin >> U >> V >> W;
        }
    }
    return 0;
}
